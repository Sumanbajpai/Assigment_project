specVersion: "v1beta"
serviceId: "party-management-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "party-management-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
